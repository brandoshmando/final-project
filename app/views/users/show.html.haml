%h1 #{@user.first_name}
= @user.type
- if @user.avatar?
  = image_tag @user.avatar.url(:medium), class: "avatar-frame"
%br
%br
%h2 Your Courses
%br
- if @user.type == "Professor" && @user.courses.empty?
  %p
    %i You do not appear to have any courses at the moment. How nice.
- else
  .vertical-tabs-container
    .vertical-tabs
      - @user.courses.each.with_index do |course, index|
        - if index == 0
          =link_to course.name, "#", class: "js-vertical-tab vertical-tab is-active", rel: "tab#{index}"
        - else
          =link_to course.name, "#", class: "js-vertical-tab vertical-tab", rel: "tab#{index}"
    .vertical-tab-content-container
      - @user.courses.each.with_index do |course, index|
        - if index == 0
          =link_to course.name, "#", class: "js-vertical-tab-accordion-heading vertical-tab-accordion-heading is-active", rel: "tab#{index}"
          .js-vertical-tab-content.vertical-tab-content{id: "tab#{index}"}
            %p= course.description
        - else
          =link_to course.name, "#", class: "js-vertical-tab-accordion-heading vertical-tab-accordion-heading", rel: "tab#{index}"
        .js-vertical-tab-content.vertical-tab-content{id: "tab#{index}"}
          %p= course.description

- if @user.type == "Professor" && @user.evals.empty?
  %p
    %i You do not appear to have any ongoing evaluations at the moment. How nice.
- else
  Your Evaluations:
  - if @user.type == "Professor"
    - @user.evals.each do |eval|
      %ul
        %li
          = link_to eval.title, eval_path(eval)