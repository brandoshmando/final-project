<!DOCTYPE html>
<html>
<head>
  <title>FinalProject</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <script type="text/javascript" src="//use.typekit.net/kyn5cgt.js"></script>
  <script type="text/javascript">try{Typekit.load();}catch(e){}</script>
</head>
<body>
<header class="centered-navigation">
  <div class="centered-navigation-wrapper">
    <a href="javascript:void(0)" class="mobile-logo">
      <img src="/assets/drrubric.png" alt="">
    </a>
    <a href="" class="centered-navigation-menu-button">MENU</a>
    <ul class="centered-navigation-menu">
      <li class="logo">
        <a href="javascript:void(0)" class="logo">
          <img src="/assets/drrubric5.png" alt="">
        </a>
      </li>
    </ul>
  </div>
</header>
<div class="new">
<% if flash[:alert] %>
  <p id="alert"><%= flash[:alert] %></p>
<% end %>
</div>
<% if current_user %>
<nav id="sidenav">
  <ul class="top">
    <div class="user-avatar">
    <li class="user-avatar">
        <%= link_to user_path(current_user), {controller: "users", action: "show"}, class: "is active" do %>
          <% if current_user.avatar? %>
            <div class="current-user-avatar-nav">
            <div class="current-user-avatar-avatar">
            <%= image_tag current_user.avatar.url(:thumb) %>
            </div>
            </div>
            <div class="current-user-avatar-name">
            <span><%= current_user.first_name %></span>
            </div>
          <% end %>
        <% end %>
    </li>
    </div>
    <li class="nav-catalog">
      <%= link_to user_path(current_user), {controller: "users", action: "show"}, class: "is active" do %>
        <i class="icon-home">
        </i>
        <span>Home</span>
      <% end %>
    </li>
    <li class="nav-catalog">
      <%= link_to user_path(current_user), {controller: "users", action: "show"}, class: "is active" do %>
        <i class="icon-notebook">
        </i>
        <span>Courses</span>
      <% end %>
    </li>
    <li class="nav-forum">
      <%= link_to user_path(current_user), {controller: "users", action: "show"}, class: "is active" do %>
        <i class="icon-paste">
        </i>
        <span>Evaluations</span>
      <% end %>
    </li>
    <li class="nav-docs">
      <%= link_to user_path(current_user), {controller: "users", action: "show"}, class: "is active" do %>
        <i class="icon-users">
        </i>
        <span>TAs</span>
      <% end %>
    </li>
  </ul>
</nav>
<% end %>
<div class="new">
<%= yield %>
<% if current_user %>
  Logged in as <%= current_user.email %>/
  <%= link_to "Log out", logout_path %>
<% else %>
  <%= link_to "Log in", login_path %>.
<% end %>
<%= debug(params) if Rails.env.development? %>
</div>
</body>
</html>
